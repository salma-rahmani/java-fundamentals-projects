public class LengthMethodExample {

    public static void main(String[] args) {
        // Declare and initialize a String variable with a sample text
        String str = "Hello, Java!";  // The string whose length will be determined

        // Print the length of the string using the length() method
        System.out.println(str.length());  // Outputs the number of characters in the string
    }
}
